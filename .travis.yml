language: generic 
sudo: true
services:
  - docker

before_install:
  - docker build -t lukaszstolarski/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true lukaszstolarski/docker-react npm run test